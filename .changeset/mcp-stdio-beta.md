---
"executor": patch
---

Add stdio MCP support via `executor mcp --stdio`, make foreground `web` and `mcp` sessions first-class CLI entrypoints, and update the dashboard MCP install card to default to the stdio install flow with an HTTP toggle.
